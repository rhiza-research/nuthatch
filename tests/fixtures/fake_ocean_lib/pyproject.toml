[project]
name = "fake-ocean-lib"
version = "0.1.0"
description = "Fake ocean library for testing nuthatch config resolution"
requires-python = ">= 3.12"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/fake_ocean_lib"]
